# دليل النشر بدون سطر أوامر (Deployment Without Terminal)

هذا الدليل مخصص للاستضافة التي **لا تحتوي على Terminal/SSH** (مثل بعض خطط Hostinger الأساسية).

---

## 📋 الخطوة 1: رفع الملفات

### رفع الملفات عبر File Manager أو FTP:

1. **ارفع جميع الملفات** إلى المجلد الرئيسي للموقع (عادة `public_html` أو `www`)
2. **تأكد من رفع:**
   - جميع ملفات Laravel
   - مجلد `app`
   - مجلد `config`
   - مجلد `database`
   - مجلد `resources`
   - مجلد `routes`
   - مجلد `storage`
   - مجلد `bootstrap`
   - ملف `composer.json`
   - ملف `artisan`
   - ملف `package.json`

3. **لا ترفع:**
   - مجلد `vendor` (سيتم إنشاؤه لاحقاً)
   - مجلد `node_modules`
   - ملف `.env` (سيتم إنشاؤه لاحقاً)
   - مجلد `storage/app/public` (يجب أن يكون فارغاً)

---

## 📋 الخطوة 2: إعداد ملف `.env`

### في File Manager:

1. اذهب إلى المجلد الرئيسي للموقع
2. ابحث عن ملف `.env.example` (إذا كان موجوداً)
3. **انسخه** وسمّه `.env`
4. **أو أنشئ ملف جديد** اسمه `.env`

### محتوى ملف `.env`:

```env
APP_NAME="ElectroPalestine"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_TIMEZONE=Asia/Gaza
APP_URL=https://electropalestine.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=اسم_قاعدة_البيانات
DB_USERNAME=اسم_المستخدم
DB_PASSWORD=كلمة_المرور

FILESYSTEM_DISK=public

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="your-email@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

CONTACT_EMAIL=your-email@gmail.com

SESSION_DRIVER=file
SESSION_LIFETIME=120

CACHE_DRIVER=file
QUEUE_CONNECTION=sync

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=error
```

**⚠️ مهم:** املأ القيم الفارغة (قاعدة البيانات، البريد الإلكتروني، إلخ)

---

## 📋 الخطوة 3: إنشاء APP_KEY عبر PHP

### الطريقة 1: استخدام Online Tool

1. اذهب إلى: https://generate-random.org/api-key-generator?count=1&length=32&type=mixed-numbers&prefix=base64
2. انسخ المفتاح
3. أضفه في `.env`: `APP_KEY=base64:المفتاح_المنسوخ`

### الطريقة 2: إنشاء ملف PHP مؤقت

1. في File Manager، أنشئ ملف جديد اسمه `generate-key.php` في المجلد الرئيسي
2. أضف هذا المحتوى:

```php
<?php
echo 'APP_KEY=' . 'base64:' . base64_encode(random_bytes(32));
```

3. افتح الملف في المتصفح: `https://electropalestine.com/generate-key.php`
4. انسخ النتيجة وأضفها في `.env`
5. **احذف الملف** بعد الاستخدام (للمزيد من الأمان)

---

## 📋 الخطوة 4: تثبيت Composer Dependencies

### إذا كان Composer متوفراً في hPanel:

1. اذهب إلى **hPanel** → **Composer** (إذا كان متوفراً)
2. اختر المجلد الرئيسي للمشروع
3. شغّل: `composer install --no-dev --optimize-autoloader`

### إذا لم يكن Composer متوفراً:

**الحل:** رفع مجلد `vendor` من بيئتك المحلية:

1. في جهازك المحلي، شغّل:
   ```bash
   composer install --no-dev --optimize-autoloader
   ```
2. ارفع مجلد `vendor` كاملاً إلى الخادم

---

## 📋 الخطوة 5: تثبيت NPM Dependencies وبناء Assets

### إذا كان Node.js متوفراً في hPanel:

1. اذهب إلى **hPanel** → **Node.js** (إذا كان متوفراً)
2. شغّل:
   ```bash
   npm install
   npm run build
   ```

### إذا لم يكن Node.js متوفراً:

**الحل:** بناء Assets محلياً ورفعها:

1. في جهازك المحلي:
   ```bash
   npm install
   npm run build
   ```
2. ارفع مجلد `public/build` إلى الخادم

---

## 📋 الخطوة 6: إعداد قاعدة البيانات

### في hPanel:

1. اذهب إلى **MySQL Databases**
2. أنشئ قاعدة بيانات جديدة (إذا لم تكن موجودة)
3. أنشئ مستخدم جديد وأعطه صلاحيات كاملة
4. سجّل المعلومات في ملف `.env`

### تشغيل Migrations:

**الطريقة 1: استخدام Online Tool**

1. أنشئ ملف `run-migrations.php` مؤقتاً:

```php
<?php
require __DIR__.'/vendor/autoload.php';

$app = require_once __DIR__.'/bootstrap/app.php';
$kernel = $app->make(Illuminate\Contracts\Console\Kernel::class);
$kernel->bootstrap();

$kernel->call('migrate', ['--force' => true]);

echo "Migrations completed!";
```

2. افتح الملف في المتصفح: `https://electropalestine.com/run-migrations.php`
3. **احذف الملف** بعد الاستخدام

**الطريقة 2: استخدام phpMyAdmin**

1. اذهب إلى **phpMyAdmin**
2. اختر قاعدة البيانات
3. اذهب إلى **Import**
4. استورد ملفات SQL من `database/migrations` (إذا كانت متوفرة)

---

## 📋 الخطوة 7: إعداد الصلاحيات (Permissions)

### في File Manager:

1. اذهب إلى مجلد `storage`
2. انقر بزر الماوس الأيمن → **Change Permissions** (أو **Properties**)
3. اضبط الصلاحيات على `775` أو `755`
4. **فعّل Recursive** ليطبق على جميع المجلدات الفرعية
5. كرر نفس الخطوات لمجلد `bootstrap/cache`

**إذا لم تكن الصلاحيات متاحة:**
- بعض الاستضافات تضع الصلاحيات تلقائياً
- جرب رفع صورة أولاً، إذا فشلت، اتصل بالدعم الفني

---

## 📋 الخطوة 8: إنشاء المجلدات المطلوبة

### في File Manager:

1. اذهب إلى `storage/app/public`
2. أنشئ المجلدات التالية:
   - `categories`
   - `types`
   - `companies`
   - `products`
   - `campaigns`
   - `ids`

3. لكل مجلد، اضبط الصلاحيات على `775` أو `755`

---

## 📋 الخطوة 9: إنشاء Storage Link

### الطريقة 1: استخدام Route بديل (موجود في الكود) ✅

**لا حاجة لعمل أي شيء!** الكود يحتوي على route `/storage/{path}` يعمل تلقائياً.

### الطريقة 2: إنشاء Symlink يدوياً

1. في File Manager، اذهب إلى مجلد `public` (أو `public_html`)
2. إذا كان مجلد `storage` موجوداً كمجرد مجلد، **احذفه**
3. انقر بزر الماوس الأيمن → **Create Symbolic Link** (إذا كان متوفراً)
4. **Target/Source**: `../storage/app/public`
5. **Link Name**: `storage`
6. انقر **Create**

### الطريقة 3: نسخ الملفات (حل بديل)

إذا لم يعمل Symlink:

1. في File Manager، اذهب إلى `storage/app/public`
2. **Select All** (حدد كل الملفات)
3. **Copy**
4. اذهب إلى `public`
5. أنشئ مجلد جديد اسمه `storage`
6. **Paste** (الصق)

**⚠️ ملاحظة:** هذا الحل يتطلب نسخ الملفات يدوياً كل مرة تُرفع صورة جديدة.

---

## 📋 الخطوة 10: إنشاء ملفات الحماية

### في `storage/app/public`:

**ملف `.htaccess`:**
1. أنشئ ملف جديد اسمه `.htaccess`
2. أضف هذا المحتوى:

```apache
Options -Indexes
<IfModule mod_headers.c>
    <FilesMatch "\.(jpg|jpeg|png|gif|webp|svg|ico)$">
        Header set Access-Control-Allow-Origin "*"
    </FilesMatch>
</IfModule>
```

**ملف `index.php`:**
1. أنشئ ملف جديد اسمه `index.php`
2. أضف هذا المحتوى:

```php
<?php
// Silence is golden
```

---

## 📋 الخطوة 11: مسح الـ Cache

### إنشاء ملف `clear-cache.php`:

```php
<?php
require __DIR__.'/vendor/autoload.php';

$app = require_once __DIR__.'/bootstrap/app.php';
$kernel = $app->make(Illuminate\Contracts\Console\Kernel::class);
$kernel->bootstrap();

$kernel->call('config:clear');
$kernel->call('cache:clear');
$kernel->call('route:clear');
$kernel->call('view:clear');
$kernel->call('optimize:clear');

echo "Cache cleared successfully!";
```

1. افتح الملف في المتصفح: `https://electropalestine.com/clear-cache.php`
2. **احذف الملف** بعد الاستخدام

---

## 📋 الخطوة 12: التحقق من النشر

### 1. اختبار الموقع:
- افتح: `https://electropalestine.com`
- يجب أن يعمل الموقع

### 2. اختبار تسجيل الدخول:
- سجّل دخول كـ Admin
- يجب أن يعمل

### 3. اختبار رفع صورة:
- أضف فئة جديدة مع صورة
- تحقق من:
  - حفظ الصورة في `storage/app/public/categories`
  - ظهور الصورة في الواجهة

### 4. فحص السجلات:
- اذهب إلى `storage/logs/laravel.log`
- تحقق من عدم وجود أخطاء

---

## 🚨 حل المشاكل الشائعة

### المشكلة 1: خطأ 500 Internal Server Error

**الحل:**
1. راجع `storage/logs/laravel.log`
2. تحقق من إعدادات `.env`
3. شغّل `clear-cache.php`

### المشكلة 2: الصور لا تُحفظ

**الحل:**
1. تحقق من صلاحيات `storage/app/public` (775 أو 755)
2. تحقق من وجود المجلدات المطلوبة
3. راجع `storage/logs/laravel.log`

### المشكلة 3: خطأ 403 Forbidden

**الحل:**
1. تأكد من وجود ملف `.htaccess` في `storage/app/public`
2. تأكد من وجود `index.php` في `storage/app/public`
3. تحقق من صلاحيات المجلدات

---

## 📝 ملاحظات مهمة

1. **احذف جميع الملفات المؤقتة** بعد الاستخدام:
   - `generate-key.php`
   - `run-migrations.php`
   - `clear-cache.php`

2. **لا ترفع ملف `.env`** إلى Git (موجود في `.gitignore`)

3. **تأكد من أن `APP_DEBUG=false`** في الإنتاج

4. **استخدم HTTPS** دائماً في الإنتاج

---

## 🔄 عند تحديث الموقع

1. ارفع الملفات المحدثة
2. شغّل `clear-cache.php`
3. إذا كانت هناك migrations جديدة، شغّل `run-migrations.php`

---

## 📞 الدعم

إذا استمرت المشاكل:
1. راجع `storage/logs/laravel.log`
2. تحقق من صلاحيات المجلدات
3. تأكد من إعدادات `.env`
4. اتصل بالدعم الفني للاستضافة
