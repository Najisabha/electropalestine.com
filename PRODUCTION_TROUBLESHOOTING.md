# دليل حل مشاكل الإنتاج (Production Troubleshooting)

## مشكلة: الصور لا تظهر في الإنتاج (electropalestine.com) بينما تعمل محلياً

إذا كانت الصور (صور الفئات، المنتجات، الشركات، إلخ) تظهر في `http://127.0.0.1:8000` ولكنها لا تظهر في `electropalestine.com`، الحل كالتالي:

### الحل السريع:

**السبب**: الصور تُحفظ في `storage/app/public` ولكن Laravel يحتاج إلى رابط رمزي (symlink) من `public/storage` إلى `storage/app/public` لعرضها.

---

## 🏢 حلول خاصة بالاستضافة المشتركة على Hostinger

### ✅ الحل النهائي: Route بديل لـ symlink (مطبوع في الكود)

**تم إضافة حل تلقائي في الكود!** 🎉

إذا لم يعمل symlink في الاستضافة المشتركة، الكود يحتوي على route خاص (`/storage/{path}`) يعرض الملفات مباشرة من `storage/app/public` بدون الحاجة إلى symlink.

**لا حاجة لعمل أي شيء إضافي** - فقط رفع الكود المحدث وسيعمل تلقائياً!

---

### الحل 1: استخدام Terminal في hPanel (للتحقق من symlink)

Hostinger يوفر Terminal في hPanel:

1. في **hPanel**، ابحث عن **Terminal** في القائمة الجانبية أو في شريط البحث
2. افتح **Terminal**
3. انتقل إلى مجلد المشروع:
   ```bash
   cd domains/electropalestine.com/public_html
   ```
   (أو المسار الصحيح حسب إعدادات Hostinger)
4. نفذ الأمر:
   ```bash
   php artisan storage:link
   ```

### الحل 2: إنشاء symlink عبر File Manager (إذا لم يتوفر Terminal)

في **hPanel**:

1. افتح **مدير الملفات** (File Manager)
2. اذهب إلى مجلد `public` (أو `public_html` حسب الإعداد)
3. **تحقق مما إذا كان مجلد `storage` موجود**:
   - إذا كان موجوداً كمجلد عادي (وليس رابط رمزي)، احذفه أولاً
   - انقر بزر الماوس الأيمن → **Delete**
4. في نفس المجلد (`public`)، ابحث عن خيار **Create Symbolic Link** أو **Symlink**
   - إذا كان متوفراً في القائمة:
     - انقر بزر الماوس الأيمن على مساحة فارغة في `public`
     - اختر **Create Symbolic Link**
     - **Target/Source**: `../storage/app/public`
     - **Link Name**: `storage`
     - انقر **Create**

### الحل 3: نسخ الملفات مباشرة (حل بديل إذا لم تفلح الطرق السابقة)

إذا لم يفلح إنشاء symlink، يمكن نسخ الملفات مباشرة:

1. في **File Manager**:
   - اذهب إلى مجلد `storage/app/public`
   - **Select All** (حدد كل الملفات والمجلدات)
   - **Copy** (انسخ)
   - اذهب إلى مجلد `public`
   - **Create New Folder** → اسمه `storage`
   - افتح مجلد `storage` الجديد
   - **Paste** (الصق)

**⚠️ ملاحظة**: هذا الحل يتطلب نسخ الملفات يدوياً كل مرة تُرفع صورة جديدة. الأفضل هو استخدام symlink.

### الحل 4: إنشاء symlink يدوياً عبر .htaccess (حل متقدم)

إذا لم تفلح الطرق السابقة، يمكن استخدام `.htaccess` لإعادة التوجيه:

1. في مجلد `public`، أنشئ ملف `.htaccess` جديد (أو عدّل الموجود)
2. أضف هذا الكود قبل القواعد الموجودة:

```apache
# Redirect storage requests to actual storage location
RewriteCond %{REQUEST_URI} ^/storage/(.*)$
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^storage/(.*)$ ../storage/app/public/$1 [L]
```

لكن **هذا الحل أقل كفاءة** من symlink.

---

### عبر SSH (إذا كان متوفر في خطتك):
```bash
cd /path/to/your/project
rm -rf public/storage  # احذف المجلد إذا كان موجوداً
ln -s ../storage/app/public public/storage
```

### ✅ التحقق من نجاح الحل:

1. **في File Manager**: تأكد من أن `public/storage` يظهر كلينك رمزي (عادة يكون له أيقونة مختلفة أو سهم)
2. **أو اختبر المسار مباشرة**: افتح في المتصفح:
   ```
   https://electropalestine.com/storage/categories/category1.png
   ```
   (استبدل `category1.png` باسم ملف صورة موجود فعلاً)
   - إذا ظهرت الصورة: ✅ الحل نجح!
   - إذا ظهر **403 Forbidden**: ❌ المشكلة في الصلاحيات أو symlink غير موجود (انظر الحل أدناه)
   - إذا ظهر **404**: ❌ الرابط غير موجود أو المسار خاطئ

3. **جرب إضافة صنف جديد مع صورة** من لوحة التحكم
4. يجب أن تظهر الصورة فوراً في الموقع

---

## 🚫 حل مشكلة خطأ 403 Forbidden

إذا ظهر لك خطأ **403 Forbidden** عند محاولة الوصول إلى `/storage/categories/category1.png`، هذا يعني أن:

1. ✅ **Symlink موجود لكن الصلاحيات خاطئة** - الحل أدناه
2. ❌ **Symlink غير موجود** - راجع الحلول أعلاه أولاً
3. ❌ **ملف .htaccess مفقود في storage/app/public** - تم إصلاحه (انظر أدناه)

### 🔧 الحل السريع لخطأ 403:

#### الخطوة 1: إنشاء ملف .htaccess في storage/app/public

في **File Manager**:
1. اذهب إلى مجلد `storage/app/public`
2. أنشئ ملف جديد اسمه `.htaccess`
3. أضف هذا المحتوى:
   ```apache
   Options -Indexes
   <IfModule mod_headers.c>
       # Allow access to image files
       <FilesMatch "\.(jpg|jpeg|png|gif|webp|svg|ico)$">
           Header set Access-Control-Allow-Origin "*"
       </FilesMatch>
   </IfModule>
   ```
4. احفظ الملف

#### الخطوة 2: إنشاء ملف index.php في storage/app/public

1. في نفس المجلد `storage/app/public`
2. أنشئ ملف جديد اسمه `index.php`
3. أضف هذا المحتوى:
   ```php
   <?php
   // Silence is golden
   ```
4. احفظ الملف

#### الخطوة 3: إصلاح الصلاحيات

جرب الحلول التالية بالترتيب:

### الحل 1: التحقق من وجود ملف `.htaccess` في مجلد `storage/app/public`

1. في **File Manager**، اذهب إلى مجلد `storage/app/public`
2. تأكد من وجود ملف `.htaccess` مع المحتوى التالي:
   ```apache
   Options -Indexes
   ```
   إذا لم يكن موجوداً، أنشئه.

### الحل 2: إصلاح الصلاحيات (Permissions)

في **File Manager**:

1. اذهب إلى مجلد `storage/app/public`
2. انقر بزر الماوس الأيمن → **Change Permissions**
3. اضبط على `755` (أو `775` إذا لم يفلح)
4. ✅ تأكد من تفعيل **Recursive** (لينطبق على جميع المجلدات الفرعية)
5. انقر **Apply**

كرر نفس الخطوات لمجلد:
- `storage/app/public/categories`
- `storage/app/public` (جذر التخزين)

### الحل 3: التأكد من أن symlink يشير للمسار الصحيح

في **File Manager**:

1. اذهب إلى `public/storage`
2. تحقق من أن الرابط يشير إلى `../storage/app/public` (أو المسار الصحيح)
3. إذا كان الرابط خاطئاً:
   - احذف الرابط الحالي
   - أنشئ رابط جديد (الحل 1 أو 2 أعلاه)

### الحل 4: التحقق من إعدادات .htaccess في public

في مجلد `public`، افتح ملف `.htaccess` وتأكد من أن القواعد التالية موجودة ولا تمنع الوصول إلى `storage`:

```apache
<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Handle X-XSRF-Token Header
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests To Front Controller...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
```

**⚠️ مهم**: القاعدة الأخيرة `RewriteCond %{REQUEST_FILENAME} !-f` تسمح بالوصول إلى الملفات الموجودة (مثل الصور).

### الحل 5: إنشاء ملف index.php للحماية في storage/app/public

1. في **File Manager**، اذهب إلى `storage/app/public`
2. أنشئ ملف جديد اسمه `index.php`
3. أضف هذا المحتوى:
   ```php
   <?php
   // Silence is golden
   ```
   هذا يمنع عرض محتوى المجلد إذا تم الوصول إليه مباشرة.

### 📝 ملاحظات مهمة:
- الرابط الرمزي `public/storage` موجود في `.gitignore` لأنه خاص بكل خادم
- يجب إنشاء هذا الرابط في كل خادم/بيئة بشكل منفصل
- **في الاستضافة المشتركة على Hostinger**: قد تحتاج إلى استخدام Terminal في hPanel بدلاً من SSH
- **إذا حدثت مشكلة في الصلاحيات** في File Manager:
  1. انقر بزر الماوس الأيمن على مجلد `storage/app/public`
  2. اختر **Change Permissions**
  3. اضبط على `775` (أو `755`)
  4. تأكد من تفعيل **Recursive** ليطبق على جميع المجلدات الفرعية

---

## مشكلة: فشل إضافة المنتجات على الخادم

إذا كانت إضافة المنتجات تعمل محلياً ولكنها تفشل على الخادم (مثل Hostinger)، تحقق من النقاط التالية:

### 1. صلاحيات المجلدات (File Permissions)

تأكد من أن مجلدات التخزين لديها صلاحيات الكتابة:

```bash
# على الخادم، قم بتنفيذ:
chmod -R 775 storage
chmod -R 775 bootstrap/cache
chown -R www-data:www-data storage
chown -R www-data:www-data bootstrap/cache
```

أو إذا كنت تستخدم Hostinger cPanel:
- انتقل إلى File Manager
- انقر بزر الماوس الأيمن على مجلد `storage`
- اختر "Change Permissions"
- اضبط الصلاحيات على `775` أو `755`

### 2. إنشاء رابط التخزين (Storage Link)

تأكد من وجود رابط رمزي من `public/storage` إلى `storage/app/public`:

```bash
php artisan storage:link
```

إذا لم يعمل الأمر، يمكنك إنشاء الرابط يدوياً في cPanel:
- تأكد من وجود مجلد `storage/app/public`
- أنشئ رابط رمزي من `public/storage` يشير إلى `storage/app/public`

### 3. التحقق من قاعدة البيانات

تأكد من:
- إعدادات قاعدة البيانات في ملف `.env` صحيحة
- المستخدم لديه صلاحيات كاملة على قاعدة البيانات
- الجداول موجودة (قم بتشغيل migrations إذا لزم الأمر):

```bash
php artisan migrate
```

### 4. التحقق من سجلات الأخطاء

بعد إضافة معالجة الأخطاء، تحقق من سجلات Laravel:

```bash
# عرض آخر الأخطاء
tail -f storage/logs/laravel.log
```

أو في Hostinger:
- انتقل إلى `storage/logs/laravel.log`
- اقرأ آخر الأخطاء المسجلة

### 5. التحقق من إعدادات PHP

تأكد من:
- `upload_max_filesize` كافٍ (على الأقل 2M)
- `post_max_size` كافٍ
- `memory_limit` كافٍ (على الأقل 128M)

يمكنك التحقق من ذلك بإنشاء ملف `phpinfo.php`:

```php
<?php phpinfo(); ?>
```

### 6. التحقق من ملف .env

تأكد من:
- `APP_ENV=production` (أو `local` للاختبار)
- `APP_DEBUG=false` في الإنتاج (للمزيد من الأمان)
- `FILESYSTEM_DISK=public` أو `local`

### 7. مسح الكاش

بعد إجراء التغييرات، امسح الكاش:

```bash
php artisan config:clear
php artisan cache:clear
php artisan view:clear
php artisan route:clear
```

### 8. التحقق من حجم الملفات

تأكد من أن حجم الصورة لا يتجاوز الحد المسموح (2MB حالياً).

### 9. التحقق من الـ Middleware

تأكد من أن المستخدم لديه صلاحيات Admin وأن الـ middleware يعمل بشكل صحيح.

## بعد إضافة معالجة الأخطاء

الآن، عند فشل إضافة منتج، ستحصل على رسالة خطأ واضحة في الواجهة، وستجد تفاصيل الخطأ في:
- `storage/logs/laravel.log`

## 💡 نصائح إضافية لـ Hostinger (الاستضافة المشتركة)

1. **استخدم Terminal في hPanel** بدلاً من SSH (عادة متوفر في جميع الخطط)
2. **استخدم File Manager** للتحقق من وجود الملفات والمجلدات ورؤية الروابط الرمزية
3. **تحقق من Error Logs** في hPanel → **Advanced** → **Error Logs** لعرض أخطاء PHP
4. **استخدم PHP Selector** في hPanel → **Advanced** → **PHP Configuration** لاختيار إصدار PHP مناسب (Laravel يحتاج PHP 8.2+)
5. **تحقق من MySQL Database** في hPanel للتأكد من اتصال قاعدة البيانات
6. **ملاحظة مهمة**: في بعض خطط الاستضافة المشتركة، قد لا تكون symlinks مدعومة. **لا تقلق!** الكود يحتوي على route بديل (`/storage/{path}`) يعمل تلقائياً بدون symlink. فقط تأكد من:
   - رفع جميع الملفات المحدثة إلى الخادم
   - التحقق من صلاحيات مجلد `storage/app/public` (755 أو 775)

## مشكلة: تعديل صورة المنتج لا يعمل

إذا حاولت تعديل صورة منتج موجود ولكن التعديل لم يتم تطبيقه:

### الحلول المحتملة:

#### 1. تحقق من صلاحيات مجلد products

في **File Manager**:
1. اذهب إلى مجلد `storage/app/public/products`
2. انقر بزر الماوس الأيمن → **Change Permissions**
3. اضبط على `755` أو `775`
4. ✅ تأكد من تفعيل **Recursive**
5. انقر **Apply**

#### 2. تحقق من وجود مجلد products

تأكد من وجود مجلد `storage/app/public/products`. إذا لم يكن موجوداً:
1. في **File Manager**، اذهب إلى `storage/app/public`
2. أنشئ مجلد جديد اسمه `products`
3. اضبط صلاحياته على `755` أو `775`

#### 3. تحقق من سجلات الأخطاء

في **File Manager**، افتح ملف `storage/logs/laravel.log` وابحث عن أخطاء تتعلق بـ:
- "فشل رفع صورة المنتج"
- "خطأ غير متوقع عند تعديل المنتج"

#### 4. تحقق من حجم الصورة

تأكد من أن حجم الصورة لا يتجاوز **2MB** (المسموح حالياً).

#### 5. تحقق من نوع الملف

تأكد من أن الملف المُرفق هو صورة بصيغة:
- `jpg` / `jpeg`
- `png`
- `gif`
- `webp`

#### 6. مسح الكاش

بعد التعديلات، امسح الكاش:

```bash
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

أو في Terminal في hPanel:
```bash
cd domains/electropalestine.com/public_html
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

---

## اختبار سريع

لاختبار ما إذا كانت المشكلة في رفع الملفات:

1. حاول إضافة منتج بدون صورة
2. إذا نجح، المشكلة في صلاحيات مجلد التخزين
3. إذا فشل، المشكلة في قاعدة البيانات أو التحقق من البيانات

## الحصول على المساعدة

إذا استمرت المشكلة، تحقق من:
- `storage/logs/laravel.log` للحصول على تفاصيل الخطأ
- رسائل الخطأ في الواجهة (بعد التحديث الجديد)
- سجلات الأخطاء في cPanel

---

## مشكلة: الصور لا يتم حفظها عند الرفع

### الحلول المطبقة في الكود

تم تحسين دالة `ImageHelper::storeWithSequentialName` لتشمل:
- ✅ معالجة أخطاء أفضل مع تسجيل مفصل
- ✅ التحقق من صلاحيات الكتابة على المجلدات
- ✅ التحقق من نجاح حفظ الملف بعد الحفظ
- ✅ رسائل خطأ واضحة في السجلات

تم إضافة التحقق من نجاح حفظ الصور في جميع الـ Controllers:
- ✅ `AdminCatalogController` (الفئات، الأنواع، الشركات، المنتجات)
- ✅ `AdminCampaignController` (الحملات)
- ✅ `AuthController` (صور الهوية عند التسجيل)

### خطوات التحقق

1. **التحقق من الصلاحيات:**
   - في File Manager، اذهب إلى `storage/app/public`
   - اضبط الصلاحيات على `775` أو `755` مع تفعيل Recursive

2. **التحقق من وجود المجلدات:**
   - تأكد من وجود: `categories`, `types`, `companies`, `products`, `campaigns`, `ids`
   - في `storage/app/public`

3. **فحص السجلات:**
   - راجع `storage/logs/laravel.log` للتحقق من الأخطاء

4. **التحقق من إعدادات PHP:**
   - تأكد من: `upload_max_filesize = 2M`, `post_max_size = 8M`, `file_uploads = On`

### إذا استمرت المشكلة

1. راجع `storage/logs/laravel.log` للحصول على تفاصيل الخطأ
2. تحقق من صلاحيات المجلدات
3. تأكد من إعدادات PHP
4. تحقق من مساحة القرص الصلب
