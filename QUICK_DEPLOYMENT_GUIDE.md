# 🚀 دليل النشر السريع بدون Terminal

## الخطوات الأساسية (بدون سطر أوامر)

### 1️⃣ رفع الملفات
- ارفع جميع ملفات Laravel إلى الخادم
- **لا ترفع:** `vendor`, `node_modules`, `.env`

### 2️⃣ إعداد ملف `.env`
- أنشئ ملف `.env` في المجلد الرئيسي
- املأ الإعدادات (قاعدة البيانات، البريد، إلخ)
- للحصول على `APP_KEY`: استخدم أداة online مثل https://generate-random.org/api-key-generator
- أو أنشئ ملف PHP مؤقت: `<?php echo 'APP_KEY=base64:' . base64_encode(random_bytes(32)); ?>`
- انسخ النتيجة وأضفها في `.env`

### 3️⃣ تثبيت Dependencies
- **Composer:** إذا كان متوفراً في hPanel، استخدمه
- **أو:** ارفع مجلد `vendor` من جهازك المحلي
- **NPM:** إذا كان متوفراً، استخدمه
- **أو:** ارفع مجلد `public/build` من جهازك المحلي

### 4️⃣ قاعدة البيانات
- أنشئ قاعدة بيانات في hPanel
- استخدم phpMyAdmin لاستيراد البيانات أو أنشئ ملف PHP مؤقت لتشغيل Migrations

### 5️⃣ الصلاحيات
- في File Manager، اضبط صلاحيات `storage` و `bootstrap/cache` على `775`

### 6️⃣ إنشاء المجلدات
- في `storage/app/public` أنشئ:
  - `categories`
  - `types`
  - `companies`
  - `products`
  - `campaigns`
  - `ids`

### 7️⃣ Storage Link
- **لا حاجة لعمل شيء!** الكود يحتوي على route بديل يعمل تلقائياً

### 8️⃣ مسح Cache
- أنشئ ملف PHP مؤقت لمسح Cache أو استخدم phpMyAdmin
- احذف الملف بعد الاستخدام

### 9️⃣ التحقق
- اختبر الموقع
- جرّب رفع صورة
- تحقق من ظهورها

---

## 📖 للتفاصيل الكاملة

راجع: `DEPLOYMENT_WITHOUT_TERMINAL.md`
